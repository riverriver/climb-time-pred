# 富士ヒルクライム タイム予測アプリ 設計仕様書

## 1. 目的

過去3ヶ月分の異なる登坂データ(FITファイルまたはIntervals.icu同期)を約10本入力するだけで、富士ヒルクライム(Mt.富士ヒルクライム/全日本マウンテンサイクリング in 富士)の完走タイムを高精度に予測する。

**設計原則:物理パラメータ(CdA・Crr・駆動効率)をユーザーに手入力させず、実走データから逆算する。** これにより「パラメータ過多で面倒」と「簡易すぎて低精度」の両立を狙う。

---

## 2. スコープ

### 対象とする効果
- 重力・転がり抵抗・空気抵抗による物理的な速度-パワー関係
- 標高上昇に伴う有酸素パワーの低下(補正可能なプラグイン方式)
- 標高上昇に伴う空気密度低下(空力項への影響)

### 明示的に対象外とする効果
- **集団走行・ドラフティング効果**:単独走行・一定出力を前提とする
- **ペーシング戦略(区間ごとの出力配分)**:登坂全体を単一の目標パワーで走ると仮定する
- 風(向かい風・追い風)、気温の走行当日変動(オプション入力として拡張可能な設計にはしておくが、初期実装では標準大気値を使用)

---

## 3. システム構成(5層アーキテクチャ)

```
[1] データ取り込み層
      ↓
[2] クライム自動抽出
      ↓
[3] パワーデュレーションモデル(PDカーブ)抽出
      ↓
[4] 物理モデル & パーソナルキャリブレーション
      ↓
[5] 富士ヒルクライム予測エンジン(標高補正込み)
```

---

## 4. データ入力仕様

### 4.1 ユーザー手入力(必須・最小限)
| 項目 | 理由 |
|---|---|
| ライダー体重 [kg] | 質量とCrrは実走データから分離不可能(多重共線性)のため必須 |
| バイク+装備重量 [kg] | 同上 |

体重・バイク重量以外の物理パラメータ(CdA, Crr, 駆動効率)は**すべて過去データから自動推定**するため、ユーザー入力に含めない。

### 4.2 走行データ(いずれか、または併用)
- **FITファイルアップロード**:`fitparse`等でパース。GPS位置・高度・速度・パワー・(あれば)気温を抽出
- **Intervals.icu API同期**:APIトークンによる認証。Strava利用規約の制約を回避できる

必須フィールド:タイムスタンプ、緯度経度、高度、速度、パワー(パワーメーター実測値)

---

## 5. クライム自動抽出アルゴリズム

1. 高度データを移動平均でノイズ除去
2. 区間平均勾配が閾値(既定3%)以上、かつ継続距離が閾値(既定1km)以上の区間を「登坂候補」として検出
3. 候補区間を結合(短い平坦・下り区間を挟んでも継続登坂とみなす許容ギャップを設定、既定200m)
4. 各登坂区間について:距離・獲得標高・平均勾配・所要時間・平均パワー・NP相当値を算出し、登坂データセットとして保存

---

## 6. パワーデュレーションモデル(PDカーブ)

- 各登坂データから、複数の継続時間(1分・5分・10分・20分・30分・60分など)におけるベストパワーを抽出
- Critical Power (CP) / W′ の2パラメータモデルをフィット:

```
P(t) = CP + W′ / t
```

- Intervals.icu連携時は、API側で既に算出済みのパワーカーブをそのまま利用可能(自前計算より優先)

---

## 7. 物理モデル

### 7.1 基礎運動方程式

```
P_wheel  = M・g・sinθ・v + Crr・M・g・cosθ・v + 0.5・CdA・ρ・v³
P_rider  = P_wheel / η
```

| 記号 | 内容 | 扱い |
|---|---|---|
| M | 総質量(ライダー+バイク+装備) [kg] | 入力値(固定) |
| g | 重力加速度 9.80665 [m/s²] | 定数 |
| θ | 勾配角度(θ = arctan(grade)) | コースデータより |
| v | 速度 [m/s] | 実測 or 予測対象 |
| Crr | 実効転がり抵抗係数 | **キャリブレーション対象** |
| CdA | 実効空力係数×前面投影面積 [m²] | **キャリブレーション対象** |
| ρ | 空気密度 [kg/m³] | 標高・気温から算出 |
| η | 駆動効率 | 既定値0.976で固定(3変数同時推定は数値的に不安定なため) |

### 7.2 空気密度モデル

国際標準大気モデル(バロメトリック公式)を使用:

```
ρ(h) = ρ0・(1 - L・h / T0)^(g・M_air/(R・L) - 1)
```

気温データがFITファイルに含まれる場合は補正に使用。ない場合は標準大気値(海面15℃)を仮定。

---

## 8. パーソナルキャリブレーション(最小二乗法)

Crr・CdA は η・M を固定値として扱うことで、以下のように**線形の最小二乗問題**に帰着できる(非線形最適化は不要):

```
η・P_rider − M・g・sinθ・v = Crr・(M・g・cosθ・v) + CdA・(0.5・ρ・v³)
```

左辺を y、右辺の係数部分を x1, x2 とすると:

```
y = Crr・x1 + CdA・x2
```

過去10本の登坂全データ点(数千〜数万点)をまとめて正規方程式(`numpy.linalg.lstsq`等)で解き、Crr・CdAを一括推定する。

**設計上の利点**:登坂ごとに勾配・速度域が異なるほど、重力項(低速で支配的)と空力項(高速で支配的)が分離しやすくなる。10本という本数・異なる山という条件は、このパラメータ分離の観点で妥当。

---

## 9. 標高別パワー減衰モデル(プラグイン方式)

過去の登坂データ(低地中心)だけでは高標高での出力低下を自己較正できないため、**独立した設定可能なモジュール**として実装する。

### 9.1 デフォルト実装

```
f(h) = 1.0                          (h ≤ h_threshold)
f(h) = 1.0 − k・(h − h_threshold)    (h > h_threshold)
```

既定値:`h_threshold = 1500 m`、`k = 0.0010 /m`(目安:1000mあたり約10%の出力低下)

```
P_sustainable(h, t) = PDカーブ(t) × f(h)
```

### 9.2 設定可能にする理由

- 高度別出力低下の係数は文献によりばらつきがあり、個人差も大きい
- ユーザーが将来的に標高のある登坂データを取得した場合、そのデータから `k` を再キャリブレーションできるよう、係数を外部設定ファイル(またはDB)に切り出す
- 複数モデル(閾値線形モデル以外の式)を将来追加できるよう、`f(h)` をインターフェース化しておく

### 9.3 空気密度側の効果(別モジュール)

標高上昇は有酸素能力を下げる一方、空気密度低下により空力抵抗を下げる(7.2節のρが物理モデルに直接効いてくる)。この2つは独立した効果として別々に実装し、混同しない。

---

## 10. 富士ヒルクライム コースデータ

- 距離・獲得標高・区間ごとの勾配プロファイル(平均勾配のみでなく、区間分解能のある高度-距離データを使用。速度のパワー式は3乗項を含み非線形なため、平均勾配での近似は誤差要因になる)
- 出典は公式コースマップまたは過去参加者のGPXを使用し、正式な数値で `docs/course_profile.md` 等に固定値として管理
- フィニッシュ標高は2000m超のため、9節の標高補正モジュールが必ず有効になる想定

---

## 11. 予測アルゴリズム(反復計算)

1. 初期タイム推定 T0(粗い推定、例:平均勾配ベースの簡易計算)
2. PDカーブから T0 に対応する海面出力 `P_sl = PDカーブ(T0)` を取得
3. 標高補正:`P_alt = P_sl × f(h_avg)`
4. キャリブレーション済み(Crr, CdA, η)+ P_alt を用い、コース区間ごとに速度を解く(aero項がv³のため各区間で3次方程式を数値的に解く、またはNewton法)
5. 区間ごとの所要時間を積算しT1を算出
6. `|T1 − T0| < 閾値(例:5秒)` になるまで2〜5を反復
7. 収束したT1を予測タイムとして出力

---

## 12. 出力仕様

- 予測タイム(点推定)
- 信頼区間(8節のキャリブレーション残差から算出)
- キャリブレーションに使われた登坂ごとの寄与度(実測タイム vs モデル逆算タイムの誤差一覧)
- 標高補正の内訳(補正前推定 / 補正後推定)を表示し、9節のモデルがブラックボックス化しないようにする

---

## 13. データモデル(概略)

| エンティティ | 主なフィールド |
|---|---|
| User | 体重、バイク重量 |
| Ride | 生FITデータ参照、日時、気温(あれば) |
| ClimbSegment | 距離、獲得標高、平均勾配、所要時間、平均パワー、親Ride参照 |
| CalibrationResult | Crr, CdA, η, 残差統計、算出日時 |
| CourseProfile(静的) | 富士ヒルクライムの区間別距離-標高データ |
| PredictionResult | 予測タイム、信頼区間、使用したCalibrationResult参照 |

---

## 14. 開発フェーズ

**Phase 1(MVP)**
FITアップロード → クライム自動抽出 → 2パラメータ線形キャリブレーション → 自前PDカーブ抽出 → 富士ヒル静的コースデータ → 標高補正(既定値)込み予測

**Phase 2**
Intervals.icu API同期、PDカーブをAPI側の値に切替、新規登坂追加時の自動再キャリブレーション

**Phase 3**
標高補正モデルの複数実装・UI上での係数調整、レース当日の気温/気圧予報入力によるρの動的補正

---

## 15. 技術スタック案

- Python
- `fitparse`:FITデコード
- `numpy` / `scipy`:最小二乗キャリブレーション、3次方程式の数値解法
- `pandas`:登坂データ管理
- Streamlit:UI
- 永続化:任意(登坂データ・キャリブレーション結果を保存する場合)
